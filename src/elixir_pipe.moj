fn add_one(x: Int) -> Int:
    return x + 1

fn double(x: Int) -> Int:
    return x * 2

fn pipeline(x: Int) -> Int:
    let s1 = add_one(x)      # x |> add_one
    let s2 = double(s1)      # |> double
    return s2
